// import { http, HtpError } from "@/lib/http"

// const users = await http.get<User[]>('/users')

// const newUser = await http.post<User>('/users', { name: 'John'})

// try {
//     await http.delete('/users/123')
// } catch(err) {
//     if (err instanceof HtpError) {
//         console.log(err.status)
//         console.log(errr.data) // Corpo da respsta
//     }
// }
